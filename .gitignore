# промежуточные токенизированные шаблоны (генерируются build/build.py,
# итог встроен в index.html как base64)
build/tokenized/
.claude/settings.local.json

# анкеты/реестры сотрудников — персональные данные, в git не попадают
анкета*.json
сотрудники*.json

# backend (Python)
backend/.venv/
backend/**/__pycache__/
backend/.pytest_cache/

# frontend (Node)
frontend/node_modules/
frontend/dist/

# секреты и локальные настройки развёртывания
.env
